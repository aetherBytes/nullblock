# Development Environment Overrides
# These values override the defaults for the development environment

environment = "dev"

# Development-specific overrides
service_overrides = {
  # Example: Override frontend port for development
  # frontend = {
  #   port = 3000
  # }
  
  # Example: Override host for local development
  # postgresql = {
  #   host = "127.0.0.1"
  # }
}
