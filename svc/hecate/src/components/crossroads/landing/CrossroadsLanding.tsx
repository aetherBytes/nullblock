import React from 'react';
import styles from '../crossroads.module.scss';

const CrossroadsLanding: React.FC = () => {
  return <div className={styles.landingView} />;
};

export default CrossroadsLanding;
